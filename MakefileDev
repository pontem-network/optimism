
docker__ports:
	sudo netstat -ntlp | grep docker | grep 0.0.0.0 | awk '{print $$4}' | cut -c9-15

test__local_optimism_sol:
	cargo test -p test-infra --features testing_local_optimism_sol

test__:
	RUST_LOG=debug cargo test -p test-infra --features testing_local_optimism_sol -- --test-threads=1
